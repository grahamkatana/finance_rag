# Worklog: 2026-09-04 — Embedder mock + Per-user data scoping

## Summary

Two major changes completed this session:

1. **Embedder mocked** — Replaced live Ollama dependency in embedder tests with an in-memory `FakeEmbedder` using bag-of-words deterministic vectors. All 5 embedder tests now pass without Ollama.
2. **Per-user data scoping** — Added `owner_id` to documents, a `document_shares` table, and scoped every feature (ingestion, retrieval, generation, audit) so items are private to their owner unless explicitly shared. Admin gets global view.

## Files Changed

### Data model
- `app/features/ingestion/models.py` — Added `owner_id` to `Document`; new `DocumentShare` model.

### Migrations
- `alembic/versions/c1d2e3f4a5b6_006_add_owner_id_to_documents.py` — Adds `owner_id` column + index to `documents`.
- `alembic/versions/d4e5f6a7b8c9_007_create_document_shares.py` — Creates `document_shares` table with unique constraint on `(file_name, granted_to_user_id)`.

### Auth / Core
- `app/core/auth.py` — Added `UserScope` model and `get_user_scope` dependency (resolves user_id + is_admin from DB).

### Ingestion
- `app/features/ingestion/service.py` — `ingest` / `ingest_with_progress` now accept `owner_id`; stored in Qdrant payload and Postgres insert.
- `app/features/ingestion/document_service.py` — Full rewrite: all methods scoped by `owner_id`/`is_admin`; added `is_owner`, `share_file`, `unshare_file`, `list_shares`.
- `app/features/ingestion/router.py` — Upload passes `owner_id`; list/delete use `get_user_scope`; new share/unshare/list-shares endpoints.

### Retrieval
- `app/features/retrieval/service.py` — `search` accepts `owner_id`/`is_admin`; Qdrant filter on owner + shared files; Postgres search scoped to owned + shared; `_fetch_chunks` validates visibility.
- `app/features/retrieval/router.py` — Uses `get_user_scope` instead of `get_current_user`.

### Generation
- `app/features/generation/router.py` — Uses `get_user_scope`; passes `owner_id`/`is_admin` to retrieval search.

### Audit
- `app/features/audit/router.py` — Non-admin `client_id` forced to own user id; admin passes through.

### Tests
- `tests/features/ingestion/test_embedder.py` — Replaced live Ollama with `FakeEmbedder` mock.
- `tests/features/ingestion/test_router.py` — Added 9 share endpoint tests.
- `tests/features/ingestion/test_service.py` — Updated all `ingest` calls with `owner_id`; new test verifying owner_id in Qdrant payload.
- `tests/features/retrieval/test_service.py` — Rewritten for scoping: handles non-admin 3-execute path; new admin vs non-admin filter tests.
- `tests/features/audit/test_router.py` — Added 5 scoping tests.
- `tests/conftest.py` — Added `get_user_scope` override + `admin_scope` fixture.

### Docs
- `AGENTS.md` — Added worklog rule: every session gets `notes/yyyy-mm-dd_HH-MM.md`.

## Test Status

```
uv run pytest tests/ -v --timeout=30
# 213 passed, 0 failed
```

All tests green.

## Open Items / Next Steps

- Run `alembic upgrade head` (requires Docker Postgres on :5436) to create the new tables.
- Verify real upload → share → search flow end-to-end with Docker running.
- Update `requests.http` with share/unshare examples.